/*
import java.util.Arrays;

public class Brains {
    // Bewaart hoeveel nummers op de juiste positie staan
    int correctPosition;


    public boolean playerGuessedCode() {
        if (Arrays.equals(arr1, arr2)) {
            return true;
        } else {
            return false;
        }
    }
    if ((antwoord.contains(code.get(i))) && (!antwoord.get(i).equals(code.get(i)))) {
                    aantalLettersJuist += 1;
                    System.out.println("aantal letters juist " + aantalLettersJuist);
                    i++;
                }

    // Checkt hoeveel letters op de juiste plaats staan. Returned int
    public int charCorrectPosition() {
        for(int i =0;i<4;i++){
            if(computer1.code.contains(userInput.charAt(i)) && !(computer1.code.get(i) == userInput.charAt(i))){
                ++correctPosition;
            }
    }
        return correctPosition;
    }


    // Checkt hoeveel letters juist zijn, maar op de verkeerde plaats staan. Returned int
}
*/
